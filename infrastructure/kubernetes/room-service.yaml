apiVersion: v1
kind: Service
metadata:
  name: room-service
  labels:
    app: room-service

spec:
  type: NodePort

  selector:
    app: room-service

  ports:
    - name: http
      port: 8081
      targetPort: 8081
      nodePort: 32081
      protocol: TCP