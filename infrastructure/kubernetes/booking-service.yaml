apiVersion: v1
kind: Service
metadata:
  name: booking-service
  labels:
    app: booking-service

spec:
  type: NodePort

  selector:
    app: booking-service

  ports:
    - name: http
      port: 8083
      targetPort: 8083
      nodePort: 32083
      protocol: TCP